{
  "name": "typing-royale-server",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "npm test && wrangler d1 migrations apply typing-royale --remote && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.134.0"
  },
  "engines": {
    "node": "^22.23.2 || ^24.21.0"
  }
}
